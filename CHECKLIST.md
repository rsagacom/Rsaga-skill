# 漫画引擎必检清单

本清单来自用户在 2026-06-06 至 2026-06-20 对 CC 漫画生成任务的连续反馈。每章交付 PDF 前必须逐项过一遍，不要只看脚本是否跑通。

## 1. 剧情完整性

- 改编层必须执行，不能直接从小说跳到生图。
- 当前唯一粒度：`1 句原文 = 1 个改编条目 = 1 格画面`。
- 不得为了减少图片数量压缩剧情、吞对白、吞旁白、吞动作。
- 每条改编必须能追溯到原文行号或句号编号。
- 核心信息必须保留：困境、人物对白、情绪转折、动作细节、伤病变化、关键道具。
- 旁白和对白要分角色，不要把所有内容混成旁白。
- 产出前对照原文检查遗漏，漏了就回改编层补，不要在排版阶段硬凑。

## 2. 角色一致性

- 开始生图前，先建立全角色外貌表。
- 主角只允许一套固定外貌描述，眼镜用细金属框，不用粗黑框。
- 每个主角画格都要带完整外貌锚点或 `{Q}`，不能只写“他/男人”。
- 回忆、童年、少年场景必须写清当时年龄，不能用成年 `{Q}`。
- 女性背影要明确“女性、长发、背影、无脸”，不能漂成短发或男性剪影。
- 房东、租客、模糊反派、无名路人默认剪影/背影/虚影，禁止幻想具体五官。
- 剪影/超自然角色必须写：无五官、无嘴、无鼻、无牙齿；不要写笑容、诡笑、嘴、鼻、牙。
- 手部画面要检查轮廓、五指、重影；不用过度写实，但必须正常。

## 3. 画风安全

- 画风必须是东亚黑白漫画、G 笔线条、2D comic/manhua。
- 禁止把 `realistic`、`photograph`、`photo-realistic`、`live-action`、`写实风格` 当正向词使用。
- 安全后缀必须保留：`NOT photograph, NOT photorealistic, NOT live-action, NOT realistic face`。
- 压缩 prompt 时不能先砍画风后缀，否则容易退回真人照片脸。
- 视觉审核必须检查真人照片感、3D 渲染感、live-action 感，不只检查“是否有角色”。

## 4. 分镜与镜头

- 分镜不能整章固定 2x2。关键剧情、情绪峰值、超自然首次出现、标题和 SFX 要用 full/hero/triple-row 等节奏布局。
- 不能大量重复主角半身/头像；要有环境、手部、物件、反应镜头、转场空镜。
- 相邻角色格情绪要渐变：平静、困惑、不安、恐惧、崩溃必须有过渡。
- 第一视角场景不该出现主角身影时，不要硬塞主角。
- 抽象画面要转成具体可见构图，不要只写“黑暗扩张”“命运感”。
- 不相干元素必须拦截：无关小孩、人群、动物、爆炸字、重影、双人叠影。

## 5. 文字与气泡

- 生图模型不得生成可读文字、帖子、报告正文、标题、门牌、SFX 字样。
- 所有标题、旁白、对白、SFX 都必须由 `BUBBLE_CONFIG`/排版层渲染。
- 角色对白使用竖排：按古籍式竖向读下去，长句拆成两列，不要左右两字两字读。
- 旁白保留底部横排条，不和角色对白规则混用。
- 对话框黑框、透明底；文字黑色加白描边。
- 字体不能太小；框和字必须匹配，不得溢出、裁切、贴边。
- 气泡不能遮住人物脸、关键动作或大半画面。
- `panel_id`、图片文件名、对白文字必须一一对应，防止排版时图文顺序错位。

## 6. 生图前拦截

- `storyboard_chXX.py` 必须先 `py_compile`。
- `audit_storyboard.py` 必须跑过，优先 GLM 5.1 做结构化审核。
- `generate_panels.py --dry-run --out <项目内 panels>` 必须通过。
- dry-run 必须硬拦：真人照片感、剪影角色五官、童年用成年主角、主角被写成病人、图内文字污染。
- 不合逻辑的 prompt 在生图前修，不要等用户看 PDF 后指出。

## 7. 生图后视觉审核

- 每张图都要用多模态模型或人工视觉审核，不要只看脚本 PASS。
- 审核真实画面是否匹配 prompt、角色外貌、年龄、服装、表情、关键动作。
- 审核是否有真人照片感、乱码文字、水印、伪 UI、重影、无关人群。
- 失败画格优先修 prompt 后重生，不能为了迁就错图改剧情。

## 8. 成页 PDF 审核

- 输出 PDF 前必须渲染页面并检查页面级可读性。
- 图文顺序必须正确，不能把某句对白贴到错误图片上。
- 文字大小、气泡位置、标题/SFX、页边距、遮脸情况都要看成页，不只看单图。
- 交付时说明：原文句子数、改编条目数、画格数、审核模型、重生画格数、PDF 路径。

最后更新：2026-06-20
